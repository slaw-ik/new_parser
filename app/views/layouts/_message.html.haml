- flash.each do |name, msg|
  %div.fade.in{:class => "alert alert-#{name == :notice ? "success" : "error"}", :style => "display:none;"}
    %a.close{"data-dismiss" => "alert"} &#215;
    = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)