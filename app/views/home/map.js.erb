var my_json = <%= raw @json %>;
var q = "<%= @q %>";

clear_directions_data();

if (q){
    console.log(q);
    show_panel(1);
}

Gmaps.map.replaceMarkers(my_json);

$('.results').text(size);
$('.search_form input.search-query').val(query);
$('#top_search').val(query);
